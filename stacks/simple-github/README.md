# Simple Github TF example


1. Generate the GITHUB token:

   - repo
   - workflow
   - delete repo (for `terraform destroy`)

2. Execute:

   ```bash
   export GITHUB_TOKEN=

   terraform fmt
   terraform apply
   ```
